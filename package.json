{
  "name": "clokk",
  "version": "0.1.0",
  "description": "A local-first CLI time tracker built for humans and AI agents",
  "type": "module",
  "bin": {
    "clokk": "./src/cli/index.ts"
  },
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "test": "bun test",
    "build": "bun build src/cli/index.ts --compile --outfile clokk",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.9"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "citty": "^0.2.1",
    "consola": "^3.4.2",
    "drizzle-orm": "^0.45.1"
  }
}
